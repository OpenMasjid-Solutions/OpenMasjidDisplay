node_modules/
dist/
# tsc's incremental cache. Regenerated by `npm run build` in web/, so it reappears as an
# untracked file after every build and would otherwise dirty the tree at every session start.
*.tsbuildinfo
*.log
.DS_Store
.env
data/
server/public/
