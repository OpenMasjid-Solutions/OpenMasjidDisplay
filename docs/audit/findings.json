{
  "audit": {
    "repo": "OpenMasjid-Solutions/OpenMasjidDisplay",
    "date": "2026-08-04",
    "commitAudited": "c1080cd",
    "treeEquivalentTo": "v0.61.0",
    "branch": "audit/security-2026-08-04",
    "autonomousPushToMain": false,
    "autonomousPushDisabledReason": "build-image.yml publishes ghcr.io/openmasjid-solutions/openmasjiddisplay:<version> and :latest on every push to main, so pushing there would ship a production container image to every masjid.",
    "baseline": {
      "serverBuild": "pass",
      "serverTests": "50/50 pass",
      "webTypecheckAndBuild": "pass",
      "serverNpmAudit": "0 vulnerabilities",
      "webNpmAudit": "1 high (postcss, build-time only)"
    },
    "counts": {
      "critical": 2,
      "high": 4,
      "medium": 11,
      "low": 11,
      "total": 28,
      "fixed": 25,
      "reportedOnly": 3
    },
    "deliverablesUpdated": "2026-08-04",
    "reportedOnly": [
      "DISPLAY-007",
      "DISPLAY-008",
      "DISPLAY-010"
    ],
    "postMergeReview": {
      "date": "2026-08-04",
      "method": "Six independent reviewers hunted for regressions introduced BY the fixes; every candidate was then handed to an independent skeptic instructed to refute it.",
      "candidatesRaised": 42,
      "survivedRefutation": 12,
      "newFindings": [
        "DISPLAY-020",
        "DISPLAY-021",
        "DISPLAY-022",
        "DISPLAY-023",
        "DISPLAY-024",
        "DISPLAY-025",
        "DISPLAY-026",
        "DISPLAY-027",
        "DISPLAY-028"
      ],
      "branch": "fix/audit-followups-2026-08-04"
    },
    "shipped": {
      "version": "0.66.0",
      "tag": "v0.66.0",
      "digestPinCommit": "715139b589f3376315bc74af919a46565e443920",
      "image": "ghcr.io/openmasjid-solutions/openmasjiddisplay:0.66.0@sha256:8b767f59821b264d819aa80022207fd6b973feac6e2acc78caffc89b58ece153",
      "date": "2026-08-05"
    }
  },
  "findings": [
    {
      "id": "DISPLAY-001",
      "title": "Unauthenticated remote crash of the entire app via a malformed cookie on the WebSocket upgrade",
      "severity": "critical",
      "confidence": "confirmed",
      "category": "denial-of-service",
      "file": "server/src/auth.ts",
      "line": 73,
      "summary": "parseCookies calls decodeURIComponent on every cookie value; a value of '%' throws URIError. On the HTTP path the handler's try/catch returns 400, but ws.ts calls the same code from inside a server 'upgrade' listener where nothing catches it and no uncaughtException handler exists, so the process exits. Demonstrated live: GET /ws with Cookie: omd_session=% killed the server and /healthz then returned ECONNREFUSED. Requires no authentication and can be looped for a sustained outage of every screen.",
      "status": "fixed",
      "commit": "08ac9f9"
    },
    {
      "id": "DISPLAY-002",
      "title": "A screen can display stale prayer times indefinitely while the panel reports it healthy",
      "severity": "critical",
      "confidence": "confirmed",
      "category": "correctness",
      "file": "server/src/render/renderPool.ts",
      "line": 79,
      "summary": "No freshness or liveness contract exists between the renderer and the screen. RenderWorker.request() has no timeout, so a hung worker never settles; renderer.ts:472 gates on this.rendering, which is only cleared in then/catch, so the pipeline stops rendering forever; writeLatest() re-feeds the last frame with no age guard; and orchestrator streamReady only measures readers>=1, so ffmpeg publishing duplicated frames keeps the screen reported online and suppresses the offline alert. A repeatedly-throwing render reaches the same state and is logged only at debug level. Result: a frozen clock and yesterday's Iqamah times on the wall with nothing raising an alarm.",
      "status": "fixed",
      "commit": "b6f2a26"
    },
    {
      "id": "DISPLAY-003",
      "title": "A truncated or corrupt session.secret fails open, making admin sessions forgeable",
      "severity": "high",
      "confidence": "confirmed",
      "category": "authentication-bypass",
      "file": "server/src/store.ts",
      "line": 176,
      "summary": "loadSecret does not validate the loaded value's length. Buffer.from(s,'hex') does not throw on malformed input and silently returns a short or empty buffer, and createHmac accepts a zero-length key, so the signing key becomes publicly known. Demonstrated live: with session.secret truncated to 0 bytes, a token signed with an empty key returned HTTP 200 from /api/state and disclosed full admin state. The precondition is reachable because the secret is written with a plain non-atomic writeFileSync, unlike db.json in the same class which uses tmp+rename.",
      "status": "fixed",
      "commit": "77e1431"
    },
    {
      "id": "DISPLAY-004",
      "title": "Production image is published using mutable third-party GitHub Action tags",
      "severity": "medium",
      "confidence": "confirmed",
      "category": "supply-chain",
      "file": ".github/workflows/build-image.yml",
      "line": 36,
      "summary": "actions/checkout@v4, docker/setup-qemu-action@v3, docker/setup-buildx-action@v3, docker/login-action@v3 and docker/build-push-action@v6 are all mutable tags in a job holding packages: write that publishes the image every masjid installs. cla.yml in this same repo is correctly SHA-pinned, and OpenMasjidAPPS pins its catalog workflow for exactly this reason, so build-image.yml is the outlier.",
      "status": "fixed",
      "commit": "e003ff6"
    },
    {
      "id": "DISPLAY-005",
      "title": "Admin session cookie is never marked Secure although the panel is served over HTTPS",
      "severity": "medium",
      "confidence": "confirmed",
      "category": "session-management",
      "file": "server/src/auth.ts",
      "line": 18,
      "summary": "The Secure attribute is added only when COOKIE_SECURE=yes, and docker-compose.yml never sets it, so it is off in every deployment. manifest.yaml opts into https: true and its own comment cites Secure cookies as a reason. Cookies are not scheme-isolated and the plain-HTTP port remains published as a legacy fallback, so a 30-day admin cookie set over HTTPS is also sent in cleartext and can be captured passively on the LAN. Setting COOKIE_SECURE globally would lock admins out of the plain-HTTP LAN flow, so the fix must mark the cookie Secure per request based on the request's scheme.",
      "status": "fixed",
      "commit": "8ff8afe"
    },
    {
      "id": "DISPLAY-006",
      "title": "Unauthenticated /api/session and /api/setup each force an outbound platform request with no throttle",
      "severity": "medium",
      "confidence": "confirmed",
      "category": "denial-of-service",
      "file": "server/src/api.ts",
      "line": 302,
      "summary": "GET /api/session is unauthenticated and calls probePlatform. With no omos_session cookie there is no caching at all, so every request performs a fresh outbound fetch to the platform (fabric.ts:221), letting one unauthenticated client flood OpenMasjidOS while holding an app socket per probe for up to 3s. POST /api/setup has the same shape and is permanently reachable under SSO because store.db.admin stays null for the life of the deployment, so its 409 short-circuit never fires. The Donations app already fixed this class; Display has not.",
      "status": "fixed",
      "commit": "3b9d815"
    },
    {
      "id": "DISPLAY-007",
      "title": "No way to revoke a session, and no password-change path exists at all",
      "severity": "medium",
      "confidence": "confirmed",
      "category": "session-management",
      "file": "server/src/auth.ts",
      "line": 42,
      "summary": "CORRECTED after the initial write-up: I first described this as \"changing the password does not sign other devices out\", but while implementing that fix I found this app has NO password-change endpoint and no password-change UI -- git grep for change.?password|newPassword|currentPassword across server/src and web/src returns nothing, and the only credential routes are /api/setup (one-shot, 409 once an admin exists) and /api/login. Tokens carry only {exp,aud} with no version, session id or server-side list, so an issued 30-day admin token cannot be revoked by ANY action available in the product, and the local password can never be changed. The only remedy for a leaked session is deleting session.secret from the data volume by hand. Not fixed here because the mechanical fix (tokenVersion bumped on password change) has nothing to hook to: making it useful means adding new authenticated endpoints plus UI, which is a product feature rather than a security patch and not something to invent unprompted mid-audit.",
      "status": "reported-only",
      "commit": null
    },
    {
      "id": "DISPLAY-008",
      "title": "Container runs the app, ffmpeg and MediaMTX as root",
      "severity": "medium",
      "confidence": "confirmed",
      "category": "container-hardening",
      "file": "Dockerfile",
      "line": 33,
      "summary": "No USER directive, so everything runs as uid 0, including ffmpeg and resvg which parse untrusted camera streams and uploaded images. docker-compose.yml sets no no-new-privileges and no cap_drop. Not fixed by this audit: adding USER changes the uid that must own /data and every existing install has a root-owned data volume, so a careless change locks masjids out of their own configuration on upgrade. Needs a deliberate migration and testing against an existing volume.",
      "status": "reported-only",
      "commit": null
    },
    {
      "id": "DISPLAY-009",
      "title": "Base images pinned by mutable tag rather than digest",
      "severity": "medium",
      "confidence": "confirmed",
      "category": "supply-chain",
      "file": "Dockerfile",
      "line": 14,
      "summary": "bluenviron/mediamtx:1.19.1 and three node:22-slim stages are tag-pinned, not digest-pinned. node:22-slim moves constantly and a version tag can be re-pushed, so two builds of the same source can yield materially different images, undercutting the @sha256 pin that docker-compose.yml relies on downstream.",
      "status": "fixed",
      "commit": "669cb2c"
    },
    {
      "id": "DISPLAY-010",
      "title": "Every push to main overwrites the published version tag and :latest",
      "severity": "medium",
      "confidence": "confirmed",
      "category": "release-integrity",
      "file": ".github/workflows/build-image.yml",
      "line": 15,
      "summary": "The workflow triggers on every push to main and derives its tag from manifest.yaml, so any merge without a version bump republishes that version tag and always overwrites :latest. A published version tag therefore stops meaning the bytes released as that version, and the @sha256 digest pin in docker-compose.yml is the only remaining integrity control. Corollary: a digest a rolled-back catalog points at becomes untagged after a rebuild and must not be garbage-collected in GHCR.",
      "status": "reported-only",
      "commit": null
    },
    {
      "id": "DISPLAY-011",
      "title": "The login limiter's sweep predicate is unsatisfiable, so its map never evicts",
      "severity": "low",
      "confidence": "confirmed",
      "category": "resource-leak",
      "file": "server/src/rateLimit.ts",
      "line": 28,
      "summary": "Entries are only created in fail(), which increments fails before storing, so every entry has fails >= 1 and the sweep condition e.fails === 0 is never true. The interval deletes nothing, defeating its own stated purpose, and one entry accumulates per distinct client IP for the process lifetime. Over IPv6 the key space is effectively unbounded, making this a slow leak on a long-running masjid box.",
      "status": "fixed",
      "commit": "6b7bf12"
    },
    {
      "id": "DISPLAY-012",
      "title": "postcss <= 8.5.22 source-map path traversal (build-time only, not reachable)",
      "severity": "low",
      "confidence": "confirmed",
      "category": "dependency",
      "file": "web/package-lock.json",
      "line": 1,
      "summary": "npm audit reports 1 high in web/ (0 in server/): postcss 8.5.15 via vite 6.4.3, advisories GHSA-r28c-9q8g-f849 and GHSA-fxqj-rqcc-2cmp, both quoted verbatim from the tool. Exploitation needs attacker-controlled CSS; this app compiles only its own in-repo stylesheets and postcss never ships in the runtime image, so it is not reachable. Fixed anyway because the fix is a one-line override and CI is part of the supply chain.",
      "status": "fixed",
      "commit": "396bbd1"
    },
    {
      "id": "DISPLAY-013",
      "title": "No security response headers on the control panel or API",
      "severity": "low",
      "confidence": "confirmed",
      "category": "hardening",
      "file": "server/src/api.ts",
      "line": 83,
      "summary": "Observed against a running server, panel and API responses carry no X-Frame-Options or CSP frame-ancestors, no Referrer-Policy and no X-Content-Type-Options. Impact is limited because SameSite=Lax denies a cross-site frame any session cookie, so clickjacking cannot reach an authenticated panel. The app already sets nosniff plus a sandbox CSP for uploaded files and a deliberate frame-ancestors * for the widget, so only the panel is unprotected.",
      "status": "fixed",
      "commit": "da46000"
    },
    {
      "id": "DISPLAY-014",
      "title": "No clock sanity check, so a wrong host clock renders confidently wrong prayer times",
      "severity": "low",
      "confidence": "confirmed",
      "category": "correctness",
      "file": "server/src/render/renderer.ts",
      "line": 479,
      "summary": "Every prayer time derives from the host clock and nothing questions it. The timezone and DST maths is correct, but a host that lost its CMOS battery, a Pi with no RTC that booted offline, or a resumed VM will render an authoritative wrong timetable. There is no 'times may be wrong' state, only the Setup needed frame for a missing location. Raise the severity on hardware without an RTC.",
      "status": "fixed",
      "commit": "5cadd87"
    },
    {
      "id": "DISPLAY-015",
      "title": "No automated dependency updates configured",
      "severity": "low",
      "confidence": "confirmed",
      "category": "dependency-hygiene",
      "file": ".github/workflows/cla.yml",
      "line": 51,
      "summary": ".github contains only build-image.yml and cla.yml; there is no dependabot.yml, so nothing notifies the maintainers when a dependency advisory lands. The cla.yml allowlist already names dependabot[bot], so the intent existed.",
      "status": "fixed",
      "commit": "703fd07"
    },
    {
      "id": "DISPLAY-016",
      "title": "print?month= accepts months 00-99 and silently renders a different month",
      "severity": "low",
      "confidence": "confirmed",
      "category": "input-validation",
      "file": "server/src/api.ts",
      "line": 829,
      "summary": "The regex constrains the shape but not the range, so ?month=2026-00 renders December 2025 and ?month=2026-99 renders a month in 2034, each labelled with whatever it landed on. No crash and no unbounded loop because the day loop is bounded by daysInMonth, but a printed calendar that is confidently the wrong month is a poor artefact to hand a congregation.",
      "status": "fixed",
      "commit": "5cac6bb"
    },
    {
      "id": "DISPLAY-017",
      "title": "The public widget is unauthenticated, uncached and unthrottled compute",
      "severity": "low",
      "confidence": "confirmed",
      "category": "denial-of-service",
      "file": "server/src/api.ts",
      "line": 253,
      "summary": "GET /w/<id> and /w/<id>.json are intentionally public for opted-in timetables. Each request computes a focus day plus seven days of prayer times and renders a full HTML page with cache-control: no-store and no rate limit, and the page polls every 30s per viewer. On a tunnel-exposed instance this is a cheap way to keep the box busy, and it shares CPU with the 1 fps render loop feeding the screens. Access control itself is sound: a disabled widget returns 404 rather than 403 so ids are not probeable.",
      "status": "fixed",
      "commit": "2d09662"
    },
    {
      "id": "DISPLAY-018",
      "title": "db.json holds credential hashes but is written with default permissions",
      "severity": "low",
      "confidence": "confirmed",
      "category": "hardening",
      "file": "server/src/store.ts",
      "line": 191,
      "summary": "db.json contains the admin scrypt hash and salt and the volunteer PIN hash, and is written with no explicit mode so umask decides. The adjacent session.secret is deliberately written with mode 0o600, so the intent exists but was not applied to the file holding the password hashes.",
      "status": "fixed",
      "commit": "4724346"
    },
    {
      "id": "DISPLAY-019",
      "title": "No container HEALTHCHECK although /healthz exists",
      "severity": "low",
      "confidence": "confirmed",
      "category": "availability",
      "file": "Dockerfile",
      "line": 86,
      "summary": "The app serves /healthz and neither the image nor docker-compose.yml uses it, leaving the orchestrator blind to a wedged-but-listening process, which is precisely the DISPLAY-002 state where the port answers and the picture is frozen. Once DISPLAY-002 lands, the endpoint should report render freshness rather than merely that the process is up.",
      "status": "fixed",
      "commit": "7534f99"
    },
    {
      "id": "DISPLAY-020",
      "title": "REGRESSION of DISPLAY-005: per-request Secure locks the admin out of the plain-HTTP panel",
      "severity": "high",
      "confidence": "confirmed",
      "category": "availability",
      "file": "server/src/auth.ts",
      "line": 9,
      "summary": "Cookies are keyed by (name, domain, path) and IGNORE the port, and this app is published on BOTH a TLS-proxied port and a plain-HTTP port of the SAME host (manifest.yaml `https: true` plus \"The plain HTTP port stays published as a legacy fallback\"); our cookies set no Domain and use Path=/. So once the HTTPS entry point stored `omd_session; Secure`, the plain-HTTP entry point could neither receive it NOR REPLACE it -- browsers refuse a non-Secure Set-Cookie that would overwrite a Secure cookie of the same name/domain/path (\"Leave Secure Cookies Alone\", RFC 6265bis 5.7). The admin typed the correct password on the HTTP port, got a 200, and bounced back to a blank login form with no in-app recovery; under SSO /api/session reported authed:true while /api/state returned 401, giving an unthrottled refetch loop on the process that runs the render loop. It bites precisely when TLS is broken, which is what the legacy port is for. My DISPLAY-005 commit claimed plain-HTTP users would \"behave exactly as before\"; that was wrong. Fixed by giving each scheme its own cookie name.",
      "status": "fixed",
      "commit": "77e6611"
    },
    {
      "id": "DISPLAY-021",
      "title": "REGRESSION of DISPLAY-017: the widget limiter throttles a masjid's own visitors",
      "severity": "medium",
      "confidence": "confirmed",
      "category": "availability",
      "file": "server/src/rateLimit.ts",
      "line": 108,
      "summary": "The limiter keyed on req.socket.remoteAddress, but every visitor arriving through the OpenMasjidOS remote-access tunnel comes from the ingress address, so all of them shared ONE 90/minute bucket -- and the widget route regex has an optional leading path segment precisely to serve that tunnel path, so the remote path is the designed path. A masjid embeds the timetable on their website and their own worshippers start 429ing each other off the prayer times. Separately, the 429 response reused SECURITY_HEADERS, swapping the widget's deliberate `frame-ancestors *` for 'self' and dropping access-control-allow-origin, so a throttled widget failed in a way the embedding page could neither frame nor read. Fixed by keying on the forwarded client (safe for a volume cap, unsafe for a credential cap -- LoginLimiter still keys on the socket) and preserving the widget's own headers on the error path.",
      "status": "fixed",
      "commit": "f30e575"
    },
    {
      "id": "DISPLAY-022",
      "title": "Incomplete DISPLAY-002: the volunteer page reported a frozen screen as ready",
      "severity": "medium",
      "confidence": "confirmed",
      "category": "correctness",
      "file": "server/src/volunteerApi.ts",
      "line": 194,
      "summary": "volunteerApi computed `ready: !!st?.streamReady` and never consulted contentStale, although the statuses it iterates already carried it; DISPLAY-002's commit never touched this file. So a volunteer standing in the hall saw a green \"ready\" for a screen displaying yesterday's Iqamah times -- the reader who most needs the truth. Fixed: ready now requires fresh content, and the payload carries `stale`.",
      "status": "fixed",
      "commit": "641d1bb"
    },
    {
      "id": "DISPLAY-023",
      "title": "Incomplete DISPLAY-002: frame age reported as ~0 when the fault is a wrong clock",
      "severity": "low",
      "confidence": "confirmed",
      "category": "correctness",
      "file": "server/src/render/renderer.ts",
      "line": 573,
      "summary": "isStale() folded two different faults together, but a wrong clock renders successfully every second, so frameAgeMs was ~0 while contentStale was true. The panel therefore read \"Times out of date - about 0 min ago\", which looks like a bug in the panel rather than a wrong clock on the box. Fixed with staleReason(): frozen quotes an age (only when at least a minute); clock suppresses it and the badge reads \"Clock is wrong\".",
      "status": "fixed",
      "commit": "641d1bb"
    },
    {
      "id": "DISPLAY-024",
      "title": "Incomplete DISPLAY-002: the alert called a dark screen \"still lit up\"",
      "severity": "medium",
      "confidence": "confirmed",
      "category": "correctness",
      "file": "server/src/orchestrator.ts",
      "line": 252,
      "summary": "Alert wording was selected on `stale` alone, so a screen that was stale AND had no decoder attached was described as \"still lit up, but its timetable stopped updating\" when it was dark. A wrong clock also got the frozen-renderer text, whose advice (\"check that screen\") is useless for a clock fault. Fixed: selection also takes litUp, and a clock fault gets its own message naming its own remedy. Wording extracted into a pure alertFor() so it is testable.",
      "status": "fixed",
      "commit": "641d1bb"
    },
    {
      "id": "DISPLAY-025",
      "title": "REGRESSION of DISPLAY-002: a recycled worker's exit killed the live worker's render",
      "severity": "medium",
      "confidence": "confirmed",
      "category": "availability",
      "file": "server/src/render/renderPool.ts",
      "line": 75,
      "summary": "`pending` is one Map for the life of the RenderWorker, shared across worker generations, but the failure handler is per worker. When the new deadline recycled a wedged worker and a replacement took over, the zombie's 'exit' still rejected and cleared the whole map, taking the replacement's in-flight render with it -- turning one wedged render into a second failed render on a screen already in trouble. Fixed with a generation guard, plus recycle() now drains the queue itself, because the guard alone would have left a second concurrent render (the shared preview worker can have several) unsettled for ever. Also documented, after measuring it, that terminate() cannot interrupt a synchronous native resvg call, so a pathological SVG still costs one core until it returns.",
      "status": "fixed",
      "commit": "6e8ffc6"
    },
    {
      "id": "DISPLAY-026",
      "title": "REGRESSION of DISPLAY-018: a leftover db.json.tmp defeated the 0600 mode",
      "severity": "low",
      "confidence": "confirmed",
      "category": "hardening",
      "file": "server/src/store.ts",
      "line": 216,
      "summary": "fs.writeFileSync applies `mode` only when it CREATES the file, so a db.json.tmp left behind by an earlier crash kept its old permissions and rename() carried them onto db.json -- the file holding the admin scrypt hash and the volunteer PIN hash. Fixed with an unconditional best-effort chmod, so an unsupported filesystem can never stop the store persisting.",
      "status": "fixed",
      "commit": "6e8ffc6"
    },
    {
      "id": "DISPLAY-027",
      "title": "No CI job ran the tests, so the audit's own ship gate was local-only",
      "severity": "high",
      "confidence": "confirmed",
      "category": "process",
      "file": ".github/workflows/build-image.yml",
      "line": 32,
      "summary": "Nothing in CI ran `npm test` or `npm run build` outside Docker. The image build compiles TypeScript inside the Dockerfile, so a type error failed the build, but not one assertion had ever executed on a runner. Every test in this repo, including all the regression guards added by this audit, had only ever run on a maintainer machine -- so the \"ship gate\" reported in this audit was local-only and a green PR was much weaker evidence than it appeared. Fixed by adding .github/workflows/checks.yml (server build+test, web typecheck+build+audit, findings.json schema validation) on pull_request and on push to main.",
      "status": "fixed",
      "commit": "702134a"
    },
    {
      "id": "DISPLAY-028",
      "title": "None of the 16 fixes reach a masjid: the catalog still pins the pre-audit image",
      "severity": "high",
      "confidence": "confirmed",
      "category": "release",
      "file": "docker-compose.yml",
      "line": 17,
      "summary": "The fixes are merged to main but NOT shipped. manifest.yaml, server/package.json and web/package.json are all still 0.61.0, and docker-compose.yml still pins sha256:3642573141cf..., which is the image built at c1080cd -- i.e. before any fix. Verified live against GHCR: the :0.61.0 and :latest tags now both resolve to a DIFFERENT digest (sha256:77f48427...) built from the audited code, while the digest the catalog serves is now reachable by no tag at all. So every masjid installing or recreating today still gets the crashable, silently-stale build. This is exactly the hazard DISPLAY-010 described, now materialised. Shipping needs the release chain: bump the four version files, tag, let CI publish, re-pin the new digest in compose, then update registry.yaml in OpenMasjidAPPS. SHIPPED as v0.66.0 (2026-08-05): four version files bumped, tagged, CI published a multi-arch image, docker-compose.yml pinned sha256:8b767f59..., and the OpenMasjidAPPS registry now points at v0.66.0. 0.66.0 rather than 0.62.0 because GHCR already had published images for 0.62.0-0.65.0. Verified the published image contains the fixes by checking its config carries the DISPLAY-019 HEALTHCHECK, which the previously-served digest does not.",
      "status": "fixed",
      "commit": "715139b"
    }
  ]
}
