{
  "name": "openmasjid-node-agent",
  "private": true,
  "version": "0.62.0",
  "description": "omd-agent — the on-device agent for an OpenMasjid Display Raspberry Pi node",
  "license": "AGPL-3.0-only",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "start": "node dist/node/agent/src/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.check.json && node --import tsx --test src/backoff.test.ts src/store.test.ts src/display.test.ts src/assetCache.test.ts src/agent.test.ts",
    "typecheck:all": "tsc -p tsconfig.check.json"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
